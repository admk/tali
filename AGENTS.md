# Agent Notes

- Run `tali --agent-cheatsheet` for an agent-oriented command reference;
  `tali -c` is the human-oriented cheatsheet.
- Run `tali` from this repository root to list todos for this project.
  - If in a worktree, `cd <main_project_root>/ && tali -i` to run it from main checkout root.

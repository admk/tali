# Agent Notes

- Run `tali -i` from this repository root to list todos for this project.
- Run `tali --agent-cheatsheet` for an agent-oriented command reference;
  `tali -c` is the human-oriented cheatsheet.
